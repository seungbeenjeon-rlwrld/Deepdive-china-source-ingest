# HTTP client. Used by the SerpApi client and the registry collectors.
requests>=2.32.0

# Reads config.yaml. If absent, the tool falls back to built-in defaults.
PyYAML>=6.0

# HTML parsing for article-body extraction (repost resolution). No JS
# execution and no browser automation.
beautifulsoup4>=4.12.0

# Reads the text out of exchange-filing PDFs. Without it the registry channel
# stops at a link that no chat-side fetch can decode.
pypdf>=4.0
